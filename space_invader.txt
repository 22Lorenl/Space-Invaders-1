# print("Hello World")
# Turtle = 500
# Cookies = Turtle
# Turtle + Cookies
from tkinter import *
import time
import random

tk = Tk()
tk.title("Ball Paddle Game")
tk.resizable(0, 0)
tk.wm_attributes("-topmost", 1)
canvas = Canvas(tk, width=500, height=400, bd=0, highlightthickness=0)
canvas.pack()
tk.update

x = True

def update_x()
  global x
  x = False
  
class Ball:
  def __init_- (self, canvas, color):
    self.canvas = canvas
    self.id = canvs.create_oval(10, 10, 25,25, fill=color)
  
  def draw(self):
    pass
    
 ball = Ball(canvas, "red")
 
 while x == True:
  tk.update_idletasks()
  tk.update()
  time.sleep(0.01)
